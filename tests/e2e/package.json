{
  "name": "@overleaf-ext/tests-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Forgejo-style Playwright e2e suite for the ext 6.3.0 Overleaf fork. Runs ONLY against the disposable test stack (docker-compose.test.yml) — never the production box.",
  "type": "module",
  "devDependencies": {
    "playwright": "1.62.1",
    "@playwright/test": "1.62.1"
  },
  "scripts": {
    "setup": "npm install --no-audit --no-fund",
    "stack:up": "bash scripts/stack-up.sh",
    "stack:down": "bash scripts/stack-down.sh",
    "stack:logs": "docker compose -f docker-compose.test.yml --project-name ol-e2e logs --tail 50 overleaf",
    "test": "npm run stack:up && npx playwright test",
    "test:tail": "npx playwright test",
    "report": "npx playwright show-report playwright-report"
  }
}
