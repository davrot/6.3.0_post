# docker image build
.dockerignore

# user defined files
.env
docker-compose.override.yml

# dependencies — never uploaded
node_modules/

# tool caches (eslint/babel/webpack)
services/web/.cache/

# yarn PnP cache + runtime artifacts (built locally, never committed — GH 100MB limit)
.yarn/cache/
.pnp.cjs
.pnp.loader.mjs
